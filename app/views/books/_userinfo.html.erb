<h1>User Info</h1>
  <div>
    <%= image_tag user.get_profile_image(100,100) %>
    <tr>
      <th>name</th>
      <th><%= user.name %></th>
    </tr>
  　
  　<tr>
  　 <th>introduction</th>
  　 <th> <%= user.introduction %></th>
  　</tr>
    
   
    <% if user == current_user %>

    <p><%= link_to edit_user_path(current_user.id) do %>
    <i class="fas fa-user-cog"></i></p>
    <% end %>
    <% end %>

  </div>